# Local GnuPG material (demo and leftover)
/keys/*
!/keys/signing.pub.asc
/keyring
/test-out/
/gpg-work/
/demo/keys/
/demo/keyring/
/demo/test-out/

# Built signer JAR (wrappers in dist/ are tracked)
/dist/*.jar

# Folder-based release stand-in
/release/signing.pub.asc
/release/artifacts/*
!/release/artifacts/.gitkeep

# Backend generated config
kmsdirect/integration.env
kmslambda/integration.env

# kmslambda: downloaded deps and build output (lockfile package-lock.json is tracked)
kmslambda/node_modules/
kmslambda/dist/
kmslambda/npm-debug.log*
kmslambda/*.tsbuildinfo
kmslambda/signing.pub.asc
kmslambda/last-signing-request.json
kmslambda/infra/.terraform/
kmslambda/infra/backend.hcl
kmslambda/infra/terraform.tfvars
kmslambda/infra/tfplan
kmslambda/infra/tfplan.json

# GitHub Actions OIDC setup dumps (filled policies, local env)
/github-actions/.local/

# Editor / OS junk
.DS_Store
*.swp
temp
